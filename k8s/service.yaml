apiVersion: v1
kind: Service
metadata:
  name: ufotracker-service
  namespace: ufology
spec:
  selector:
    app: ufotracker-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP