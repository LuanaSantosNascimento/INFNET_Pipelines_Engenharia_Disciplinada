apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: ufology
spec:
  selector:
    app: redis-cache
  ports:
    - protocol: TCP
      port: 6739
      targetPort: 6739
  type: ClusterIP