apiVersion: v1
kind: Service
metadata:
  name: ufodb-service
  namespace: ufology
spec:
  selector:
    app: ufodb
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP